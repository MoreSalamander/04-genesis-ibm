# Demo video script — Genesis OS: Enterprise Decision Intelligence (IBM track)

**Target ≤ 3:00.** Record the governance chamber (:3030). Every beat is live-verified
(decisions.md 2026-08-14). Interleave your Bob session recordings — the built-with-Bob
evidence is this track's pass/fail gate, so it gets real screen time.

| Time | On screen | Say |
|---|---|---|
| 0:00–0:15 | Chamber masthead; header shows `policy engine: 1`. | "Every studio has a room where money gets decided. This is ours — Enterprise Decision Intelligence, built for the IBM track. And its heart, the policy engine, wasn't written by us: it was **built by IBM Bob**." |
| 0:15–0:40 | CUT TO Bob IDE recordings: the Session-1 plan, Bob writing packs.py, tests going green. | "We reserved the engine — the code that judges compliance — for Bob. It planned, implemented, and when our review caught a fail-open bug, Bob fixed its own work under follow-up. Every session is on the record: plans, diffs, tests." |
| 0:40–1:05 | Click **SUBMIT THE FLAGSHIP** — $2.8M render-farm expansion. Scores compute; Gemini rationales stream in, citing evidence ids. | "A $2.8M capital proposal with its evidence package. Criterion scores are computed in code from the evidence — Gemini writes the reasoning around them, never the numbers." |
| 1:05–1:30 | Policy findings land: **vendor-concentration VIOLATION** — observed share 0.287 vs 0.25 cap, full math visible. Verdict banner: **BLOCKED**. | "Bob's engine checks the studio's written policies. HelioCompute would cross the vendor-concentration cap — twenty-eight point seven percent against a cap of twenty-five. Governance says BLOCKED, and no one — human or AI — can approve past a standing violation." |
| 1:30–1:55 | Type note + `NimbusRender` in the amend box → **REQUEST REVISION**. Round-2 flag; findings re-judge: COMPLIANT at 0.10. | "So the Studio Head revises — amending the vendor *on the record*. The durable workflow loops, governance re-judges the amended proposal: compliant at ten percent." |
| 1:55–2:20 | **AUTHORIZE** → AUTHORIZED; the Software Objective card appears (Render Queue Manager configuration). | "Authorized — at the escalated tier the budget band demands. And here's the part that makes this enterprise software, not a chatbot: the approved decision becomes an **Authorized Software Objective**…" |
| 2:20–2:45 | CUT TO Bob Session-4 recording: Bob executing the objective; back to chamber — action DELIVERED with evidence refs. | "…and IBM Bob executes it. The decision the room made becomes working software, delivered with evidence attached to the dossier. Boardroom to codebase, governed end to end." |
| 2:45–3:00 | Repo README built-with-Bob table; evidence folder; (if live) Confluent topic with ledger events. | "Policy in code. Authority with humans. Construction with Bob. Genesis OS — Enterprise Decision Intelligence." |

**Recording notes**
- A full live round takes ~1–2 min (Gemini) — pre-run round 1, record from findings on,
  or time-compress with cuts.
- The spoken-line feature (UI-ALIVE-SPEC §3) belongs at the 1:05 BLOCKED moment if
  implemented: the chamber announcing its own verdict is the clip people remember.
- Requires Bob S4 done (the delivery beat) — coordinate with 05's flagship (same
  objective, two sides).
