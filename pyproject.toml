[project]
name = "genesis-ibm"
version = "0.1.0"
description = "Genesis OS — Enterprise Decision Intelligence. Standalone decision-governance system, built with IBM Bob (IBM track, Google Cloud Agentic Cinema Hackathon)."
readme = "README.md"
requires-python = ">=3.11"
license = { text = "MIT" }
dependencies = [
    "fastapi>=0.115",
    "uvicorn[standard]>=0.30",
    "pydantic>=2.7",
    "httpx>=0.27",
    "python-dotenv>=1.0",
    "pyyaml>=6.0",
    "google-genai>=1.0",
    "acryl-datahub>=1.0",
    "psycopg[binary]>=3.2",
    "nats-py>=2.7",
    "temporalio>=1.7",
    "redis>=5.0",
    "minio>=7.2",
    "confluent-kafka>=2.5",
    "opentelemetry-sdk>=1.25",
    "opentelemetry-exporter-gcp-trace>=1.6",
]

[project.optional-dependencies]
dev = ["pytest>=8.0"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["app"]
